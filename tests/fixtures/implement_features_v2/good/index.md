---
final_test_cmd: python3 -c "pass"
---

# Fixture feature (v2)

Minimal shared context so `implement_features_v2` can be imported in unit tests
without depending on whatever happens to sit in the repo's `tmp/` directory.
